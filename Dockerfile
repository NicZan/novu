# Use a imagem base do Redis Alpine
FROM redis:alpine

# Você pode adicionar instruções adicionais se necessário, como copiar configurações ou definir variáveis de ambiente.
# Por exemplo:
# COPY redis.conf /etc/redis/redis.conf
# CMD ["redis-server", "/etc/redis/redis.conf"]

# As instruções acima são apenas um exemplo. O conteúdo exato do Dockerfile dependerá dos requisitos específicos do seu serviço.
